map-sync.ps1:25 | clause 2 | `gh issue view ... --jq .body` captured into `$body` variable (not redirected to a file); multi-line stdout becomes a PowerShell array of lines | any issue body spanning more than one line, e.g. `"## Summary\ntext"`
map-sync.ps1:27 | clause 2 | `Set-Content -NoNewline -Value $body` where `$body` is the array from line 25; `-NoNewline` joins an array with no separator, collapsing every line break | same multi-line body as above — bodyFile ends up as one run-on line with no `\n` between former lines
map-sync.ps1:31 | clause 3 | `[regex]::Matches($existing, '^## ')` where `$existing` is `Get-Content` output (array of lines); PowerShell stringifies the array by joining with a space, so `^` only ever matches at the very start of the whole blob | body with headings on line 1 and later, e.g. `"## A`ntext`n## B"` — only the first heading (if any) is ever counted, real count of 2 reported as 0 or 1
map-sync.ps1:39 | clause 1 | `gh issue edit ... --body $updated` passes the decision text through argv instead of `--body-file`; PowerShell/CreateProcess splits on internal whitespace | `-Decision "ship the fix"` gets shredded into separate argv tokens, `gh` receives a truncated/garbled `--body` value
map-sync.ps1:43 | clause 2 | second `gh issue view ... --jq .body` again captured straight into `$after` variable instead of a file, same array-of-lines exposure as line 25 | multi-line body again arrives as an array, not a string
map-sync.ps1:44 | clause 4 | verification only checks `$after -like "*$Decision*"` (phrasing), never confirms the rest of the body/structure survived the round trip | a write that corrupts everything except the decision bullet (e.g. via the line-39 shredding) still makes this check print "Map #$num updated."
spawn-agent.ps1:36 | clause 1 | `$denyList` is one string with internal spaces passed as a single `-ArgumentList` element; `Start-Process` builds the child command line by joining array elements with spaces and does not auto-quote elements containing spaces, so the deny list is split into multiple argv tokens | `$denyList = "Bash(git push:*) Bash(rm:*) Bash(gh pr merge:*)"` arrives at `claude` as five separate arguments instead of one `--disallowedTools` value, corrupting the deny rules
