# Audit: map-sync.ps1, spawn-agent.ps1 vs RULE.md

map-sync.ps1:25 | clause 2 | `gh ... --jq .body` output is captured into `$body` as an array of lines, and that same array is what gets written back at line 36/39, so the body's line structure is only whatever a later join happens to impose. | Any map body with more than one line — a 25 KB map with 40 sections.
map-sync.ps1:27 | clause 2 | `-NoNewline` on the array in `$body` joins the elements with no separator at all, concatenating every line of the map into one line in body.md. | A body of `## Frontier` + `- ticket 7` becomes `## Frontier- ticket 7`.
map-sync.ps1:30 | clause 3 | `Get-Content` without `-Raw` returns an array, so `[regex]::Matches($existing, '^## ')` at line 31 stringifies/enumerates in a way where `^` never anchors per line; the count comes back 0 (or matches only the first element) and the integrity check reads as a result rather than a check. | A map whose first line is `# Map` and whose `## ` headings all appear later — the guard throws (or passes) for reasons unrelated to whether the body arrived intact.
map-sync.ps1:39 | clause 1 | The whole updated map body is passed on argv via `--body $updated`; the multi-line string is handed to the native `gh` through the command line instead of `--body-file`, where newlines and quoting are re-parsed. | A decision bullet containing a newline, or any map body with blank lines — the pushed body is truncated at the first line break.
map-sync.ps1:44 | clause 4 | The post-write check is a substring/phrasing test (`-like "*$Decision*"`) on the round-tripped body; it passes as long as the decision text appears anywhere, including in a body that has been flattened to a single line or had its sections destroyed. | The line-27 flattening plus a successful edit: `$after` contains the decision text, the script prints "Map #N updated.", and the map is ruined.
spawn-agent.ps1:30 | clause 1 | `$denyList` is one string containing spaces, and `Start-Process -ArgumentList` splits it on those spaces, so `claude` receives `Bash(git`, `push:*)`, `Bash(rm:*)`, ... as separate argv entries and the deny rules do not parse. | `"Bash(git push:*) Bash(rm:*) Bash(gh pr merge:*)"` — `git push` is left allowed for the headless agent.
