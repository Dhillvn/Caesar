map-sync.ps1:27 | clause 2 | `$body` captured via `$body = gh ... --jq .body` assignment (not `-Raw`), so PowerShell splits multiline stdout into an array; `Set-Content -NoNewline` on that array joins elements with no separator, collapsing the body to one run-on line | body with 2+ paragraphs, e.g. `"## Plan\nline one\nline two"` becomes `"## Planline oneline two"` on disk.
map-sync.ps1:31 | clause 3 | `$existing = Get-Content $bodyFile` (no `-Raw`) is an array of lines; `[regex]::Matches($existing, '^## ')` stringifies the array by joining with `$OFS` (a space), so `^` only ever matches at the very start of the whole joined blob | a valid map body with headings `## Plan` and `## Notes` on lines 1 and 5 reports `headingCount = 1` (or 0 if `## Plan` isn't literally the first line), even though the body is intact.
map-sync.ps1:39 | clause 1 | `gh issue edit $num --repo "$owner/$repo" --body $updated` passes body content straight through argv instead of `--body-file`, so PowerShell's command-line re-quoting can split/mangle it on internal spaces or newlines | Decision text `"Ship v2 now"` (contains spaces) or any body with embedded newlines gets truncated/split when handed to `gh` via argv.
map-sync.ps1:44 | clause 4 | The write is verified only by `$after -like "*$Decision*"` — checking that the new phrase appears, not that the surrounding structure (headings, prior content) survived the round trip | if line 27's `-NoNewline` corruption or line 39's argv split mangled everything *except* the appended `- $Decision` bullet, this check still reports "Map #$num updated." on a destroyed body.
spawn-agent.ps1:36 | clause 1 | `$denyList` ("Bash(git push:*) Bash(rm:*) Bash(gh pr merge:*)", one string with internal spaces) is passed as a single `-ArgumentList` element to `Start-Process`, which rebuilds the native command line by joining args with spaces and does not re-quote embedded-space elements, so the claude CLI receives it split into separate argv tokens | claude sees `--disallowedTools Bash(git push:*)` with `Bash(rm:*)` and `Bash(gh pr merge:*)` as stray positional args instead of one deny list, silently dropping the `rm` and `gh pr merge` denials.
