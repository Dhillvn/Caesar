map-sync.ps1:25 | clause 2 | `gh issue view ... --jq .body` output captured into `$body` variable instead of redirected to a file, so multiline output becomes a PowerShell line array before ever reaching disk | issue body with 2+ paragraphs
map-sync.ps1:27 | clause 2 | `Set-Content -NoNewline -Value $body` where `$body` is an array of lines (from line 25) joins them with no separator | multi-line issue body collapses into one run-on line
map-sync.ps1:31 | clause 3 | `[regex]::Matches($existing, '^## ')` applied to `$existing`, an array from `Get-Content`, which PowerShell stringifies by joining elements with a single space before matching, so `^` can only ever match at the very start of the whole blob | issue body with a `## Heading` on any line but the first: headingCount comes back 0 and the script throws, or undercounts and misses real corruption
map-sync.ps1:39 | clause 1 | `gh issue edit $num --repo "$owner/$repo" --body $updated` passes the full body text as a CLI argument instead of `--body-file` | a `$Decision` containing a newline or a `"` truncates/misparses the argument before gh receives the intended text
map-sync.ps1:44 | clause 4 | `if ($after -like "*$Decision*")` only checks that the decision substring appears somewhere in the returned text, not that the document structure is intact | a failed edit that leaves the old body untouched but where `$Decision` text happens to already appear elsewhere (or got appended to a truncated/mangled body) still reports "updated"

spawn-agent.ps1:36 | clause 1 | `$denyList` (`"Bash(git push:*) Bash(rm:*) Bash(gh pr merge:*)"`, a single string with internal spaces) is passed as an argv element via `-ArgumentList` to the `claude` executable instead of through a file | `--disallowedTools` value gets split on its internal spaces by the downstream CLI parser, same failure mode as bug #19, silently admitting `git push`/`rm`/`gh pr merge`
