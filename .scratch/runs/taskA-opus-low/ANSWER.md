# Audit — map-sync.ps1, spawn-agent.ps1 vs RULE.md

map-sync.ps1:25 | clause 2 | `gh --jq .body` output is captured into `$body` as a string array (one element per body line) and that variable is what later gets written back, so every downstream use is already operating on a split-up value instead of the real body text. | A map body of 200 lines becomes a 200-element array before anything else touches it.
map-sync.ps1:27 | clause 2 | `Set-Content -NoNewline` joins the `$body` array with no separator, so the saved file is every line concatenated end-to-end with the newlines gone. | Body `## Frontier` + `- ticket 12` writes as `## Frontier- ticket 12`.
map-sync.ps1:31 | clause 3 | `Get-Content` (no `-Raw`) returns an array, `[regex]::Matches` stringifies it into one space-joined line, so `^## ` can only ever match at the very start and the count comes back 0 or 1 regardless of how many sections exist — the integrity gate never fires. | A body with 6 `## ` sections that has just been flattened by line 27 still yields count 1 and passes.
map-sync.ps1:39 | clause 1 | The new body goes to `gh issue edit` as an argv value; PowerShell's native-command argument handling splits it on whitespace and the CLI takes only the first token as `--body`, dropping the rest or reading it as extra args. | Decision `- picked option B for latency` writes a body ending `- picked` and errors on the trailing words.
map-sync.ps1:44 | clause 4 | The confirmation is a substring match on the decision text, which is satisfied by a body that arrived truncated or line-flattened, so a destroyed map reports "updated". | Line 27 flattens all headings away but the appended decision string is present, so the check passes on a wrecked body.
spawn-agent.ps1:30 | clause 1 | The deny list is one string containing spaces and is passed through argv to `claude`, so it splits at each space and only `Bash(git push:*)` is bound to `--disallowedTools` while the remaining rules land as stray positional args. | `"Bash(git push:*) Bash(rm:*) Bash(gh pr merge:*)"` leaves `rm` and `gh pr merge` un-denied.
