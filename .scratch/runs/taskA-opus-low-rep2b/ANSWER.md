# Audit: map-sync.ps1 / spawn-agent.ps1 vs RULE.md

map-sync.ps1:25 | clause 2 | `gh ... --jq .body` output is captured into `$body` as an array of lines, and that array is the value later written back at line 39, so line structure is already lost before any writing happens. | A map body of 200 lines: `$body` becomes a 200-element array, not the original text.
map-sync.ps1:27 | clause 2 | `Set-Content -NoNewline` on the array `$body` joins the elements with no separator at all, flattening the whole body into a single line in body.md. | Body `"## Frontier`n- ticket 4"` lands on disk as `## Frontier- ticket 4`.
map-sync.ps1:31 | clause 3 | `Get-Content` without `-Raw` returns an array, and `[regex]::Matches` on an array stringifies it into one space-joined line, so the `^## ` anchor matches at most once and normally zero times — the integrity check reports a clean result on a destroyed body. | A body with three `## ` sections yields `$headingCount` of 0 or 1, so the guard either throws on a good map or passes on a shredded one.
map-sync.ps1:39 | clause 1 | The new body goes to `gh` through argv (`--body $updated`); PowerShell splits the multi-line, space-bearing value on whitespace, so `gh` receives only the first token as the body and the rest as stray arguments. | `-Decision "ship the parser"` makes `$updated` multi-line; `gh` gets `--body <first-line-fragment>` and errors or truncates the map.
map-sync.ps1:44 | clause 4 | The post-write check is a substring match for the decision phrase, which succeeds as long as that phrase survived — it says nothing about whether the rest of the map is still there. | Body overwritten with only `- ship the parser`: `-like "*ship the parser*"` is true and the script prints "Map updated" over a wiped map.
spawn-agent.ps1:30 | clause 1 | The deny list is one space-separated string handed to `Start-Process -ArgumentList`, which splits it on its internal spaces, so `--disallowedTools` receives only `Bash(git` and the remaining fragments become separate arguments — the guardrails silently do not apply. | `"Bash(git push:*) Bash(rm:*) Bash(gh pr merge:*)"` arrives as `--disallowedTools Bash(git` plus junk; `rm` and `gh pr merge` stay allowed.
